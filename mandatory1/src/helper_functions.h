#ifndef HELPER_FUNCTIONS_H
#define HELPER_FUNCTIONS_H

char** matrix_2d (int, int);
char** matrix_zeros (int, int);
int* array_zeros (int);
int* array_neg_one (int);
void printmat (char**, int, int);
void printvec (int*, int);

#endif
